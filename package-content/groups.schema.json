{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SteelPermissionGroupsConfig",
  "description": "Permission group configuration schema for Steel",
  "type": "object",
  "properties": {
    "default_groups": {
      "type": "array",
      "description": "Groups applied to every player",
      "items": {
        "$ref": "#/definitions/group_name"
      },
      "uniqueItems": true,
      "default": ["default"]
    },
    "groups": {
      "type": "object",
      "description": "Permission groups keyed by group name",
      "minProperties": 1,
      "required": ["op"],
      "additionalProperties": {
        "$ref": "#/definitions/group"
      }
    }
  },
  "required": ["groups"],
  "additionalProperties": false,
  "definitions": {
    "group_name": {
      "type": "string",
      "pattern": "^[a-z0-9_-]+$"
    },
    "domain_name": {
      "type": "string",
      "pattern": "^[a-z0-9_.-]+$"
    },
    "world_identifier": {
      "type": "string",
      "description": "Namespaced loaded world id such as lobby:spawn",
      "pattern": "^[a-z0-9_.-]+:[a-z0-9_.-]+$"
    },
    "permission_key": {
      "type": "string",
      "description": "Dotted permission key. Only a full trailing segment may be a wildcard.",
      "pattern": "^(\\*|[a-z0-9_-]+(\\.[a-z0-9_-]+)*(\\.\\*)?)$"
    },
    "permission_rule_expression": {
      "type": "string",
      "description": "Permission key with optional context selector, such as steel.fly{domain=lobby}.",
      "pattern": "^(\\*|[a-z0-9_-]+(\\.[a-z0-9_-]+)*(\\.\\*)?)(\\{([a-z0-9_-]+|[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*)=[^\\s{},=]+(,([a-z0-9_-]+|[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*)=[^\\s{},=]+)*\\})?$"
    },
    "permission_value_key": {
      "type": "string",
      "description": "Namespaced permission metadata key.",
      "pattern": "^[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*$"
    },
    "permission_metadata_expression": {
      "type": "string",
      "description": "Permission metadata key with optional context selector, such as plugin:homes{domain=survival}.",
      "pattern": "^[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*(\\{([a-z0-9_-]+|[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*)=[^\\s{},=]+(,([a-z0-9_-]+|[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*)=[^\\s{},=]+)*\\})?$"
    },
    "permission_value": {
      "description": "Typed permission metadata value",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "integer"
        },
        {
          "type": "string"
        }
      ]
    },
    "group": {
      "type": "object",
      "properties": {
        "priority": {
          "type": "integer",
          "description": "Conflict priority used when equally specific rules from multiple groups match. Higher priority wins.",
          "default": 0
        },
        "allow": {
          "type": "array",
          "description": "Permission rule expressions explicitly allowed by this group",
          "items": {
            "$ref": "#/definitions/permission_rule_expression"
          },
          "uniqueItems": true,
          "default": []
        },
        "deny": {
          "type": "array",
          "description": "Permission rule expressions explicitly denied by this group",
          "items": {
            "$ref": "#/definitions/permission_rule_expression"
          },
          "uniqueItems": true,
          "default": []
        },
        "metadata": {
          "type": "array",
          "description": "Ordered permission metadata rules with expression keys",
          "items": {
            "$ref": "#/definitions/metadata_rule"
          },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "metadata_rule": {
      "type": "object",
      "properties": {
        "key": {
          "$ref": "#/definitions/permission_metadata_expression"
        },
        "value": {
          "$ref": "#/definitions/permission_value"
        }
      },
      "required": ["key", "value"],
      "additionalProperties": false
    }
  }
}
