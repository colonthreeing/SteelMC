{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SteelPermissionGroupsConfig",
  "description": "Permission group configuration schema for Steel",
  "type": "object",
  "properties": {
    "default_groups": {
      "type": "array",
      "description": "Groups applied to every player",
      "items": {
        "$ref": "#/definitions/group_name"
      },
      "uniqueItems": true,
      "default": ["default"]
    },
    "groups": {
      "type": "object",
      "description": "Permission groups keyed by group name",
      "minProperties": 1,
      "required": ["op"],
      "additionalProperties": {
        "$ref": "#/definitions/group"
      }
    }
  },
  "required": ["groups"],
  "additionalProperties": false,
  "definitions": {
    "group_name": {
      "type": "string",
      "pattern": "^[a-z0-9_-]+$"
    },
    "domain_name": {
      "type": "string",
      "pattern": "^[a-z0-9_.-]+$"
    },
    "world_identifier": {
      "type": "string",
      "description": "Namespaced loaded world id such as lobby:spawn",
      "pattern": "^[a-z0-9_.-]+:[a-z0-9_.-]+$"
    },
    "permission_key": {
      "type": "string",
      "description": "Dotted permission key. Only a full trailing segment may be a wildcard.",
      "pattern": "^(\\*|[a-z0-9_-]+(\\.[a-z0-9_-]+)*(\\.\\*)?)$"
    },
    "permission_value_key": {
      "type": "string",
      "description": "Namespaced permission metadata key.",
      "pattern": "^[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*$"
    },
    "permission_context_key": {
      "description": "Custom permission context key. Namespaced keys are preferred for plugin-owned contexts.",
      "anyOf": [
        {
          "$ref": "#/definitions/group_name"
        },
        {
          "type": "string",
          "pattern": "^[a-z0-9_-]+(\\.[a-z0-9_-]+)*:[a-z0-9_-]+([./][a-z0-9_-]+)*$"
        }
      ]
    },
    "permission_value": {
      "description": "Typed permission metadata value",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "integer"
        },
        {
          "type": "string"
        }
      ]
    },
    "group": {
      "type": "object",
      "properties": {
        "priority": {
          "type": "integer",
          "description": "Conflict priority used when equally specific rules from multiple groups match. Higher priority wins.",
          "default": 0
        },
        "allow": {
          "type": "array",
          "description": "Permission keys explicitly allowed by this group",
          "items": {
            "$ref": "#/definitions/permission_key"
          },
          "uniqueItems": true,
          "default": []
        },
        "deny": {
          "type": "array",
          "description": "Permission keys explicitly denied by this group",
          "items": {
            "$ref": "#/definitions/permission_key"
          },
          "uniqueItems": true,
          "default": []
        },
        "rules": {
          "type": "array",
          "description": "Structured permission rules with optional contexts",
          "items": {
            "$ref": "#/definitions/rule"
          },
          "default": []
        },
        "values": {
          "type": "array",
          "description": "Structured permission values with optional contexts",
          "items": {
            "$ref": "#/definitions/value_rule"
          },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "rule": {
      "type": "object",
      "properties": {
        "key": {
          "$ref": "#/definitions/permission_key"
        },
        "state": {
          "type": "string",
          "enum": ["allow", "deny"]
        },
        "context": {
          "$ref": "#/definitions/rule_context"
        }
      },
      "required": ["key", "state"],
      "additionalProperties": false
    },
    "value_rule": {
      "type": "object",
      "properties": {
        "key": {
          "$ref": "#/definitions/permission_value_key"
        },
        "value": {
          "$ref": "#/definitions/permission_value"
        },
        "context": {
          "$ref": "#/definitions/rule_context"
        }
      },
      "required": ["key", "value"],
      "additionalProperties": false
    },
    "rule_context": {
      "type": "object",
      "description": "Rule-side context where this permission applies. Multiple selectors are combined with AND.",
      "properties": {
        "domain": {
          "$ref": "#/definitions/domain_name"
        },
        "world": {
          "$ref": "#/definitions/world_identifier"
        },
        "custom": {
          "oneOf": [
            {
              "$ref": "#/definitions/custom_context"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/custom_context"
              },
              "minItems": 1
            }
          ]
        }
      },
      "minProperties": 1,
      "additionalProperties": false
    },
    "custom_context": {
      "type": "object",
      "description": "Plugin or subsystem-defined permission context",
      "properties": {
        "key": {
          "$ref": "#/definitions/permission_context_key"
        },
        "value": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": ["key", "value"],
      "additionalProperties": false
    }
  }
}
